server:
  port: 8282
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/transaction
    username: user
    password: password
  jpa:
    show-sql: true
    open-in-view: false
  liquibase:
    change-log: classpath:liquibase/changelog-master.yml